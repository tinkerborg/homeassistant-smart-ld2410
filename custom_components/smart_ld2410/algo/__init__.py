"""Pure-Python radar feature pipeline for the Smart LD2410 integration.

This package must not import homeassistant or anything outside the standard
library, and must not read wall-clock time — all time comes from Frame
timestamps so recorded data can be replayed deterministically.
"""
